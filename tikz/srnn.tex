\documentclass{ctexart}
\usepackage{avanti}
\everymath{\color{Solarized-magenta}}
\pagestyle{empty} % 没有页眉和页脚

\tikzset{font=\large}

% define the plot style and the axis style
\tikzset{base/.style={smooth,thick,Solarized-base01,draw=Solarized-base01}}
\tikzset{arrow/.style={->,>=stealth,base}}
\tikzset{curve/.style={base,Solarized-blue}}
\tikzset{nc/.style={base,circle,minimum width=32,fill=Solarized-base2}}
\tikzset{box/.style={base,rectangle,minimum height=0.8cm,minimum width=1.4cm}}
\tikzset{box2/.style={box,fill=Solarized-base2}}

\begin{document}

\begin{tikzpicture}

    \foreach \i in {3,5,...,13}
        {
            \pgfmathtruncatemacro{\index}{(\i-1)/2};
            \ifthenelse{\i=11}
            {
                \node [box,draw=none] (y\index) at (\i,6) {...};
                \node [box,draw=none] (a3\index) at (\i,4.5) {...};
                \node [box,draw=none] (a2\index) at (\i,3) {...};
                \node [box,draw=none] (a1\index) at (\i,1.5) {...};
                \node [box,draw=none] (x\index) at (\i,0) {...};
                \pgfmathtruncatemacro{\im}{\index-1};
                \draw [arrow] (a1\im) -- (a1\index);
                \draw [arrow] (a2\im) -- (a2\index);
                \draw [arrow] (a3\im) -- (a3\index);
            }
            {
                \ifthenelse{\i=13}
                {
                    \node [box2] (y\index) at (\i,6) {$\hat{y}_T$};
                    \node [box] (a3\index) at (\i,4.5) {$\av_T^{(3)}$};
                    \node [box] (a2\index) at (\i,3) {$\av_T^{(2)}$};
                    \node [box] (a1\index) at (\i,1.5) {$\av_T^{(1)}$};
                    \node [box2] (x\index) at (\i,0) {$\xv_T$};
                }
                {
                    \node [box2] (y\index) at (\i,6) {$\hat{y}_\index$};
                    \node [box] (a3\index) at (\i,4.5) {$\av_\index^{(3)}$};
                    \node [box] (a2\index) at (\i,3) {$\av_\index^{(2)}$};
                    \node [box] (a1\index) at (\i,1.5) {$\av_\index^{(1)}$};
                    \node [box2] (x\index) at (\i,0) {$\xv_\index$};
                }
                \draw [arrow] (x\index) -- (a1\index);
                \draw [arrow] (a1\index) -- (a2\index);
                \draw [arrow] (a2\index) -- (a3\index);
                \draw [arrow] (a3\index) -- (y\index);
                \ifthenelse{\NOT 3 = \i}
                {
                    \pgfmathtruncatemacro{\im}{\index-1};
                    \draw [arrow] (a1\im) -- (a1\index);
                    \draw [arrow] (a2\im) -- (a2\index);
                    \draw [arrow] (a3\im) -- (a3\index);
                }{;}
            }
        }

\end{tikzpicture}




\end{document}

