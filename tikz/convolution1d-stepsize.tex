\documentclass{ctexart}
\usepackage{avanti}
\everymath{\color{Solarized-magenta}}
\pagestyle{empty} % 没有页眉和页脚

\tikzset{font=\normalsize}

 % define the plot style and the axis style
\tikzset{base/.style={smooth,thick,draw=Solarized-base01}}
\tikzset{arrow/.style={->,>=stealth,base}}
\tikzset{curve/.style={base,Solarized-blue}}
\tikzset{nc/.style={circle, base}}

\begin{document}

\begin{tikzpicture}
    \foreach \i in {1,...,7}
        {
            \node [nc] (x\i) at (2*\i,0) {$x_{\i}$};
        }

    \foreach \i in {1,...,3}
        {
            \node [nc] (a\i) at (4*\i,-2) {$a_{\i}$};
        }

    \foreach \i in {1,...,3}
        {
            \pgfmathtruncatemacro{\ip}{2*\i-1};
            \pgfmathtruncatemacro{\ipp}{2*\i};
            \pgfmathtruncatemacro{\ippp}{2*\i+1};
            \draw[arrow] (x\ip) -- (a\i);
            \draw[arrow] (x\ipp) -- (a\i);
            \draw[arrow] (x\ippp) -- (a\i);
        }
\end{tikzpicture}





\end{document}

